import React from "react";
import { AppBar, Toolbar } from "@mui/material";

export default () => {

    return (
        <AppBar
            position="fixed"
            elevation={1}
            color="primary"
        >
            <Toolbar>
                test
            </Toolbar>
        </AppBar>
    )
}