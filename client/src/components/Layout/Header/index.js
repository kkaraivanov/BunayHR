import React from "react";
import { AppBar, Toolbar } from "@mui/material";

export default () => {

    return (
        <AppBar
            position="sticky"
            elevation={0}
            color="primary"
        >
            <Toolbar>
                test
            </Toolbar>
        </AppBar>
    )
}