import { AppBar, Toolbar } from "@mui/material"


export default () => {

    return (
        <AppBar
            position="fixed"
            elevation={0}
            color="primary"
        >
            <Toolbar>
                test
            </Toolbar>
        </AppBar>
    )
}